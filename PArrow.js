export function Arrow()
{
      var names = ["Tom", "Ivan", "Jerry"];
      var arrObj =[];
     // names.map(value=>arrObj.push({Name:value,length:value.length}));
          var ans=names.map(value=>arrObj.push({Name:value,length:value.length}));
      console.log(arrObj);
}
